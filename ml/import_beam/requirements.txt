# Pinned to what this package was built and tested against (2026-09-04).
#
# setuptools MUST be < 70 BEFORE these are installed. Three of apache-beam's
# dependencies (crcmod, dill 0.3.1.1, hdfs) still ship as source distributions
# that call setuptools APIs removed in 70; with a newer setuptools the install
# fails part-way and leaves a venv that imports beam but cannot run a pipeline.
# setup_env.sh does this in the right order — use it rather than a bare
# `pip install -r requirements.txt` into a fresh venv.

setuptools<70
wheel

apache-beam==2.68.0
netCDF4==1.7.4
numpy==2.2.6
PyYAML==6.0.3
requests==2.34.2
xarray==2026.7.0
copernicusmarine==2.4.1

# crcmod is already an apache-beam dependency and is what computes the
# TFRecord checksums. Named here so nobody removes it as "unused".
crcmod

# Tier 2 only — ERA5 through the Climate Data Store. Harmless to install now;
# the fetcher refuses politely until CDSAPI_KEY exists.
cdsapi

# NOT INSTALLED, and not needed: tensorflow. beam_import reads and writes
# TFRecord and tf.train.Example with its own code (tfrecord.py, example.py),
# because DESIGN §4 promises a reader with no TensorFlow import at all.
# `pip install tensorflow-cpu` works and the tests cross-check against it when
# it is present, but it upgrades protobuf past what apache-beam pins — so it
# stays out of the default environment. See BUILD_NOTES.

# tests
pytest==9.1.1
