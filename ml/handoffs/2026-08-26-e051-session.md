# Session handoff — the E-051 launch session (2026-08-25 ~19:00Z → 2026-08-26 ~06:00Z, written ~06:00Z)

For the next agent, whichever account it runs in. Read `ml/CLAUDE.md` first
(it governs), then diff reality against `claude/expectations.md` in the
claude.ai project (§5.27 — its ~00:20Z state is this session's; the hourly
watch rewrites it and its copy is the live one). This session ran the
operator-approved "go straight to the big TPU run" and closed the E-045
ladder; the concurrent overnight session built E-052 (see its own handoff,
`ml/handoffs/2026-08-25-e052-overnight.md`).

## The one thing to protect

**E-051 (`e051-k144-full`, the full-budget K=144 2-year-context pentad
stage-2 head, 200k×256 on a v5litepod-4) is TRAINING and lands ≈14:00Z
08-26.** Chris approved the plan for the finished head explicitly ("Agreed,
please go ahead" / "Costs are fine, please continue"):

1. **Verify the finals shipped** to `gs://earth-tpu-staging/runs/e051-k144-full/`
   (final `temporal_e051_jax.npz` + torch-loadable `temporal_e051.pt` +
   `metrics.jsonl`) — the node self-deletes after its EXIT trap ships them;
   anything not on the bucket does not survive the reap.
2. **Publish the .pt to `model-checkpoints-v1`** so a dispatch can name it.
3. **ROLL FIRST** — the registered authority reading. Stage `sroll` over the
   published head, E-044b-roll protocol: horizon 73, 3 starts per holdout
   year, data `family4_na025_pentad_r2`, ONLY `horizon_auc_daymatched` on
   `corridor_trainlon` comparable — judged against the monthly pair's +0.939
   and E-044b's pentad **−0.499**. Gate uncertified at pentad by construction
   (first reading). Precedent: #433's dispatch (`plan-433.json` on
   `ml-metrics`; spec `claude/E044-pentad-stage2-spec.md` §7a/§7b). ~3–5 h,
   ~$1–1.5, on a cheap GPU box. Write the §0d dispatch entry BEFORE
   dispatching.
4. **The continuation decision returns to Chris** with the final 30k curve
   slope — NOT pre-approved. Resume from the bucket npz is a true
   continuation (optimizer + schedule position restored; relaunch same node
   name with a larger `STEPS` total), ≈$50/100k steps; LR is ≈3e-5 at 200k
   (expdecay halflife 40k), so a long-halflife fresh dispatch is the cleaner
   instrument if the curve demands a genuinely bigger run.

State at 05:50Z: step 80,000/200,000 · val_zmse **0.97119** (ratio-equiv
0.0453 vs monitor persistence 21.44621) and still falling (1.181@50k →
0.971@80k) · clean pace 0.166 s/step, **all-in 0.275 s/step** (monitor evals
every 2k + in-training probe every 20k — the $90/08:30Z projection priced
clean steps only; the corrected figure ≈$155/14:00Z is on the record at
26299bb and accepted by Chris) · clip fired only in warmup, nonfinite 0 ·
checkpoints ship every 10 min. **Cross-framework sanity: E-051's val at 20k
(1.741) sat right on #478's torch 20k final (1.753) at the same geometry —
tiers stay UNPOOLED (§3b), read as a direction.**

## What this session did (chronological, all on main)

1. **E-051 launched through four nodes** — the two real bugs it surfaced are
   fixed on main and the fixes are the session's durable value:
   - Node 1 (~$0.20): launcher disk guard refused 90 GB free vs NEED_GB 120.
     Fix folded into the launcher: tmpfs fallback (remount /dev/shm to 170G
     when the host has ≥160 GB RAM).
   - Node 2 (~$4.50): **K=144 HBM staging OOM** — `put()` staged the full
     10.95 GB monitor batch onto device 0 via `jnp.asarray` before
     re-sharding. Fix [0f04c3e](https://github.com/blauewelt/earth/commit/0f04c3ee0346f132720ec3a0a99fa178f1344197):
     `jax.device_put(numpy, NamedSharding)` slices host-side.
   - Node 3 (~$9): **host-starved input pipeline** — ~15 s/step of
     single-threaded numpy `standard_normal` (428M draws) + ~2.2 s
     gather+cast against ~0.3 s of TPU step; >4.2 s/step, chips ~95% idle.
     Fix [c4ce2da](https://github.com/blauewelt/earth/commit/c4ce2da36):
     four flag-gated knobs — `--noise-backend device` (jax RNG stream,
     recorded in config), `--gather-fp16` (exact), `--gather-workers 8`,
     `--prefetch 2` — all default-off in the trainer, all ON in the TPU
     launcher. Guards: `tests/test_jaxport_pipeline.py` P1–P3 (P3 proves two
     full CLI runs with flags on/off write IDENTICAL loss curves); all 5 G5
     gates re-run green.
   - Node 4: **25× faster** (0.169→0.166 s/step clean), running since
     22:35Z 08-25.
2. **E-045 CLOSED at the decisive rung.** #478 (K=144 torch twin) finished
   23:33:26Z: one-step ratio **1.7418/21.2544 = 0.0820**, deep in the
   pre-registered 0.07–0.15 context band. The factorial's verdict: with span
   fixed at 720 d the ratio is 0.071–0.082 at 30/10/5-day steps — flat,
   against the K-fixed 0.07→0.51 cliff. Archive complete (probes-478.json,
   all EIGHT files). Papered: abstract's old "difficulty is intrinsic to the
   step size" reading RETIRED for the span story; sec:factorial completed;
   fig_factorial re-made; tab:ablations resolved; both PDFs rebuilt,
   delivered, pushed ([ec448b0](https://github.com/blauewelt/earth/commit/ec448b084)).
   `ml/figures/fig_factorial2.py/.png` regenerated (now saves beside itself,
   not to /home/claude).
3. **E-045.3 BLOCKED, the A/B answered.** #484 cancelled 23:53Z: ~57 min into
   stage 2 with ZERO step records + gpu 0%/cpu 96% on two samples — the exact
   #476 signature, on the other side of a full box stop/start. **Config-tied,
   not box-tied; never dispatch a third copy.** The unblocking affordance is
   the slice-the-published-Z proposal (read the complete published
   `Z_8b639abe36_37e146384b`, take rows `ts`, write a normal cached Z the
   trainer memmaps — also deletes the ≈$4.5/rung strided embed tax). Offered
   to Chris; his call to build or park. Box 48632885 stopped after a queue
   check (0 queued; no sole copies). Cost of #484 ≈$7.5; E-045.3 total 2
   copies ≈$18, zero step records.
4. **Operator approvals recorded** (26299bb): roll-first for E-051's head;
   the corrected ≈$155 cost accepted ("Costs are fine").

## In flight / queued at ~06:00Z (verify, don't trust)

- **E-051** — see "the one thing to protect". 200k ≈14:00Z.
- **#483 (E-047-HEAD 6th, fusion-vs-selection verdict)**: TL;DR — does one
  month-block embedding beat picking the best pentad? Running on
  gpu-box-48254133 since #478's 23:33Z drain; re-embeds the month-block Z
  (~3.5 h — no `header_t` 516 Z exists on ANY release; sole copy is stopped
  Vast 47724565, NEVER DESTROY). **Watch embed-cache-v1 for header_t-516
  chunks during its embed** — publishing them closes the gap permanently;
  absence after ~20 min of embedding (dated from the SECOND embedding record,
  never step-21 start) is a §5.26 failure. Expect a THREE-file probe bundle
  (the month-block probe gap is KNOWN — probe_kfold/dip_check/probe_head
  never pass the block map, probe_sequence 3-vs-2 unpack; Chris's fix, open).
  Verdict vs A2a's 0.0721 ≈12:00–18:00Z.
- **#480 (E-049a d_z-6 width-tax control, other session's wave)**: 200k
  ≈04:15Z + finals — likely landing now; harvest belongs to its session /
  the hourly watch. **Gates E-050's first `--fsq-warmstart` dispatch**
  (affordance at 6c83ed3; recipe must set `fsq_warmstart`+`resume`+
  `fsq_levels`; plan doc owed before dispatch; coordinate via EXPERIMENTS —
  it is the other session's wave).
- **30-day artifacts expiring 2026-09-24**: pixelmae-472 / -473 / -477 —
  publish to the release or write off deliberately.

## Scheduled wakes (bound to THIS session — a new session re-creates what it
needs; the hourly fleet watch is account-level and survives)

- **06:30Z** (trig_01MaVJvBS54f8A6Ndtycmwna): overnight verdicts — #483
  embed + 516-chunk watch, #480 harvest check, E-051 pace.
- **08:45Z** (trig_01LXqStnkbuUVpsR3FgSwi9v): E-051 mid-morning check; its
  prompt carries the full roll-first execution plan and re-schedules the
  harvest wake to the live-projected 200k time.

## Traps this session hit (beyond ml/CLAUDE.md §7)

- **A TPU pace measured at one geometry does not transfer to another whose
  host work is 50× larger.** And the all-in pace (with monitor evals +
  probes) can be 1.6× the clean s/step — price ETAs from `stage2_wall_s`
  deltas across a probe boundary, not from clean segments.
- **A DELETING TPU node holds the 4-core quota until fully gone** — poll
  `tpu_box.py list` for "no TPU nodes" before a create retry.
- **A node that dies in setup ships its log via the EXIT trap** — read
  `runs/<node>/logs/` before any retry. A run whose setup + first
  `--ckpt-every` steps exceed STALL_MIN self-reaps while healthy — check the
  arithmetic at every launch (node 4 cleared it: ~28 min vs 90).
- **The `Infinity` token in results JSON** (7th sighting, probes-478):
  replace before JSON.parse.
- **The rescue-pair metrics (`rescued-orphan-metrics-latest-<n>`) are the
  stage-1 mirror's, truncated early** — the full curve archive for a finished
  run is `run-<n>.jsonl` on `ml-metrics`.
- **ml-live branch history holds only minutes** (force-push every ~15 s) —
  you cannot bracket an event's time from its commits; use artifact
  timestamps.
- **secret-handoff ran twice this night** (22:13Z, 23:41Z, actor blauewelt,
  workflow_dispatch) — attributed to the hourly watch minting the GCP SA key
  for TPU-bucket reads. Expected shape; flag only a run with no session to
  own it.
- Two `git pull --rebase` collisions with concurrent sessions on
  EXPERIMENTS.md — re-read your own block after every rebase (§ lore); one
  needed a manual conflict merge keeping BOTH sessions' blocks.

## Access (nothing new; pointers only)

GitHub PAT `/home/claude/.gh_pat` (push via `scripts/git_api_push.mjs` when
the proxy refuses); GCP SA key `/home/claude/.gcp_sa.json` (recipe:
`claude/gcp-tpu-access.md` — secret-handoff workflow; project
`earth-tpu-blauewelt`, bucket `gs://earth-tpu-staging`, zone `us-west1-c`,
quota 4 v5e cores); Vast key `/home/claude/.vast_key` (fleet scripts under
`scripts/`); credentials docs live in the claude.ai project only.
