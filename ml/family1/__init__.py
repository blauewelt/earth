"""Families 1.gf, 1.0.tf and 0.9.tf (E-082) — adapters and layouts.

`ml/build_family1_stores.py` is the CLI; `family1.adapters.REGISTRY` is the
set of stores it can build. See `ADAPTER_CONTRACT.md` in this directory.
"""
