.DS_Store
node_modules/
*.log
_vendor/
test-results/
playwright-report/

# stray bytecode from running the bake/icon scripts locally
__pycache__/
*.pyc

# draft paper lives in the claude.ai project, not the public repo (submission privacy)
# paper build intermediates only — the sources and PDFs are tracked
ml/paper/*.aux
ml/paper/*.log
ml/paper/*.out
ml/paper/*.fls
ml/paper/*.fdb_latexmk
ml/paper/*.synctex.gz

# session tooling: agent worktrees land here when a sandbox session uses
# worktree-isolated subagents; never repo content
.claude/
ml/cache/pangeo-cmip6.csv
